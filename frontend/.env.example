# Where the API lives. Leave unset for local development — requests stay
# relative and Vite's dev proxy forwards them to the backend on :8000.
# Set this in Vercel to the deployed backend's origin, with no trailing slash.
# VITE_API_BASE=https://your-service.onrender.com
